<h1>Music</h1>
<div>
 
</div>
<br>
<br>

<% if current_user %>
<div align="center"><%= link_to 'Add Music', new_tune_path %></div><br>
    <h1>MUSIC REVIEW COMING SOON!!!!</h1>
          <%= link_to 'Remove', method: :delete, data: { confirm: 'Are you sure?' } %>
        </div>
      </div>
    
<% end %>

<div class="row">
  <div class="col-md-8">
    <% @tunes.each do |tune| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to tune.name, tune %>
          </h4>
          <small>
            <%= tune.description %>
          </small>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-md-4">
    
     

  </div>


