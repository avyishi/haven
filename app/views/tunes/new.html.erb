<h1>New Music</h1>

<%= simple_form_for @tune, html: { :multipart => true } do |f| %>
 <div class="form-inputs">
  <%= f.label :title %>
  <%= f.text_field :title %> 
  <%= f.label :image %>
  <%= f.submit "Update", class: 'btn btn-info'  %>
</div>
<% end %>

<%= link_to 'Back', tunes_path %>