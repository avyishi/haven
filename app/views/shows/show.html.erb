<h1><%= @show.title %></h1> 
<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4">
        <%= image_tag @show.image.url(:medium) %>
         <div class="table-responsive">
            <table class="table">
              <tbody>
                <tr>
                  <td><%= @show.title %></td>
                </tr>
              </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>


<%= link_to "Edit", edit_show_path(@show), class: 'btn btn-success' %>


 
