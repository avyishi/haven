<%= form_for(@reviewtune) do |f| %>
  <% if @reviewtune.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reviewtune.errors.count, "error") %> prohibited this reviewtune from being saved:</h2>

      <ul>
      <% @reviewtune.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
